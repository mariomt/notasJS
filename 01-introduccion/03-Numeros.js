var numero = 5;

// suma
numero += 1; // numero = numero + 1;

// resta
var rest = numero - 5;

// multiplicación
var mul = numero * 2;

// división
var div = numero / 2;

// flotantes
var flot = 20.3;

// Dos decimales (lo devuelve en string)
var floatstr = flot.toFixed(2);

//convertir a float
var flot = parseFloat(flot);